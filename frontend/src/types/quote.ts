export type Quote = {
  id: number;
  vehicleId: number;
  driverAge: number;
  yearsLicensed: number;
  penaltyPoints: number;
  durationMinutes: number;
  pricePence: number;
  expiresAt: string;
  createdAt: string;
};